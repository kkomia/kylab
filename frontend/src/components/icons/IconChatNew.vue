<script setup lang="ts">
/**
 * IconChatNew 图标——「新建对话」（内联 SVG，见《前端设计规范》§3）。
 *
 * **这是第二处"来源由用户点名"的例外**（第一处是 `IconSidebar`）：用户要求取
 * 阿里巴巴矢量图标库的图标。iconfont.cn 的下载要登录、拿不到可构建期复现的源文件，
 * 所以改从其同一体系的开放集 **Ant Design Icons**（Ant Group / 阿里巴巴官方设计
 * 体系的图标集，MIT）取 `plus-circle` 的几何——圆圈加号：既表达"新建"，又比方框
 * 加号圆润（第一版取的是 `plus-square`，用户反馈"太硬朗"）。
 *
 * 与 `IconSidebar` 同一套适配口径，三件事必须一起做，少一件就把别的坐标系带进来：
 * 1. **坐标网格**：Ant 的 viewBox 是 `64 64 896 896`，`IconBase` 统一 `0 0 24 24`。
 *    注意**不是**铺满 0..24——Ant 的图标是满格画的，而本仓基准集 Remix 的内容都落在
 *    2..22；直接铺满会比旁边的图标大一圈。所以映射到 2..22：
 *    `translate(2 2) scale(20/896) translate(-64 -64)`（先里层平移、再缩放、再外层平移）；
 * 2. **颜色**：Ant 生成器会带死 `fill`，这里一律不留，颜色只由 `currentColor` 决定；
 * 3. 去掉 `p-id` / `t` / `class` 等生成器属性，只留几何。
 *
 * 因此这不算"混用图标库"：运行时没有外链、没有字体、没有第二套坐标系，
 * 只是把一组几何搬进了本仓的 24 网格。
 */
import IconBase from './IconBase.vue'
</script>

<template>
  <IconBase v-bind="$attrs"
    ><g transform="translate(2 2) scale(0.022321428571428572) translate(-64 -64)"
      ><path
        d="M696 480H544V328c0-4.4-3.6-8-8-8h-48c-4.4 0-8 3.6-8 8v152H328c-4.4 0-8 3.6-8 8v48c0 4.4 3.6 8 8 8h152v152c0 4.4 3.6 8 8 8h48c4.4 0 8-3.6 8-8V544h152c4.4 0 8-3.6 8-8v-48c0-4.4-3.6-8-8-8z" /><path
        d="M512 64C264.6 64 64 264.6 64 512s200.6 448 448 448 448-200.6 448-448S759.4 64 512 64zm0 820c-205.4 0-372-166.6-372-372s166.6-372 372-372 372 166.6 372 372-166.6 372-372 372z" /></g
  ></IconBase>
</template>

# 变更记录

本项目遵循[语义化版本](https://semver.org/lang/zh-CN/)。

---

## [未发布]

**Agent 从"很会读"变成"会动手"**：工具面补上四处缺口（读文件、执行、表格 SQL、
定时任务），另有一次交互修复、一次文档重组、一个桌面外壳与一条"超限后可续跑"的出路。
逐条落地记录（含验收与门禁数字）在[开发计划 §12.205–§12.213](docs/计划与记录/开发计划-v0.1.md)。
**发版动作**：按文末「版本号约定」把三处手写版本号与 compose 的默认标签一起改到 `0.3.0`，
再把这一节挪成 `## [0.3.0] — <日期>`（这一批没有顺手改版本号：默认镜像标签一变，
`docker compose pull` 就会去找一个还没构建出来的 tag）。

### 新增能力

- **超限后可续跑**（v0.32）：撞上步数或时间上限时，界面上多一个「继续」——
  模型接着上一轮**已经查到的资料**往下做，而不是从头重发（重发是"回退一轮 + 重来"，
  已经花掉的检索与半截正文全部作废）。做法是从落库的快照重建上下文
  （回合内的工具上下文不入库），并把上一轮的出处接进这一轮的来源账本——
  编号必须与交给它的交接说明一致，否则它写出来的 `[n]` 会指向别的资料。
- **读文件 / 执行 / 表格 SQL / 定时任务**（v0.33，见[设计 §6.6](docs/设计/Agent-工作区与能力层设计-v0.1.md)）：
  `list_files` / `read_file` / `search_files` 读工作区与沙箱（只读、路径受 `resolve_in` 约束）；
  `run_command` 在沙箱里执行（**管理员专属 + 策略闸 + 内核隔离**，默认档 `ask` 下不执行、
  只告诉你怎么放开，没有隔离时拒绝而不是裸跑）；`list_tables` / `query_table` 给表格副本
  一条**只读 SQL**（单条语句、不许碰文件与扩展、表名受库范围约束）；
  定时任务（cron / 一次性，按服务器时区解释）到点自动跑一轮问答，结果落进一条会话。
- **工作区根目录改成"选"而不是手输**（v0.35）：弹窗里多一个「浏览…」，在**服务器上**
  的目录树里挑（管理员专属）。这件事必须在服务端做——浏览器里的目录选择器给的是
  客户端的路径，而后端跑在 NAS 上，选出来的东西它用不了。数据目录会出现在列表里但
  标着不可选与原因（判定与建工作区**同一份**），所以灰掉的一定也建不出来。
  选择器里还能**新建文件夹**与**重命名**（内联一行输入，建完直接进去）——
  这两件事是在服务器上写东西，所以比浏览严一档：只建一层、重名当场拒、名字按可移植的
  那一套校验；改名只改名不搬位置，且四类目录会被拒（含"包含数据目录的那个目录"
  与"某个工作区的根目录"）。
- **桌面端套壳**（v0.30，`desktop/`）：Tauri 2 的薄壳，只做"打开服务器地址"——
  后端仍跑在 NAS 上（见[桌面端套壳调研](docs/调研/桌面端套壳调研-v0.1.md)）。
  **首个发行版 v0.1.0 已构建**（2026-09-21）：绿色版 7.7 MB、
  Windows 安装包 2.1 MB（简体中文 / English）、MSI 3.2 MB，产物路径见
  [`desktop/README.md`](desktop/README.md#打包安装包)。
- **新 logo 与单色图标**（v0.31）：环形行星 + 小写 kylab 矢量版，界面与 favicon 统一。

### 修复

- **复制按钮失效**（v0.29）：根因是 `navigator.clipboard` 在文档失焦时抛
  `NotAllowedError`，现在按"剪贴板 API → `execCommand` 兜底 → 选中内容"三级降级。
- **降级标记没有落库**（v0.32）：刷新一次页面"这次没跑完"的提示就没了，
  而它恰恰是回看旧回答时最该知道的一件事。现在落库，并带出可续跑的入口。
- **头像签名 URL 的 flaky 用例**：过期时间戳跨一秒会让两次请求的链接字符串不同，
  改成比路径并真的 GET 一次。

### 工程

- **docs/ 按类型分文件夹**（v0.29）：规范 / 设计 / 调研 / 计划与记录 / 归档，
  相对链接逐条核对过。
- **工具循环的第二道闸**（v0.32）：整轮墙钟（默认 300 秒）——子 Agent 早就是
  "轮次 + 墙钟 + 检索次数"三道独立闸，主循环只有步数一道，而步数挡不住"某一步卡很久"。
- **过程快照的收法提到服务层**（`agent.step_snapshot`）：定时任务那条链路要落同一份，
  复制一份必然分叉。
- 部署里显式设了 `TZ`（默认 `Asia/Shanghai`）：容器默认 UTC，而定时任务按服务器本地时间解释。

### 破坏性变更

- 无。存储 schema 前进一版（迁移 v12 建 `scheduled_tasks`），启动时自动应用。

## [0.2.0] — 2026-09-17

**一次破坏性的存储更换，加上 0.1.0 之后全部工作的合集**。此后逐条工作此前只存在于代码里，
没有任何版本号承接（0.1.0 的版本号一直挂了两周多）；这个版本把它落下来。
唯一的破坏性变更在存储层，其余都是新增能力。

逐条落地记录（含验收与门禁数字）在[开发计划 §11–§12](docs/计划与记录/开发计划-v0.1.md)，
这里只按能力族归并。

### 破坏性变更

- **存储层由 SQLite 换成 PostgreSQL，PG 成为必选依赖**。SQLite 实现整体删除
  （`app/storage/sqlite_impl/` 已空），未配置 `KYLAB_DATABASE_URL` 时服务启动即失败，
  不再有可回退的本地实现。向量改 pgvector（HNSW、按知识库分区），
  全文改 tsvector 生成列 + GIN + jieba，DuckDB 与对象存储（本地 / S3）保留。
  **升级影响**：必须先准备一个带 pgvector 的 PostgreSQL；0.1.0 宣称的
  「单机零中间件」自此作废。按语义化版本，这次破坏性切换意味着下一个版本号应为 `0.2.0`。

### 新增能力

- **笔记板块**：Tiptap 富文本（图文混排 / 待办 / 占位引导），Markdown 为唯一事实源，
  AI 处理、图片插入、自动保存，`attach_note` 一键入库走摄入流水线。
- **知识库 Wiki**：库形态开关 + 带出处的页面树。
- **记忆层**：人设文件（四份，落磁盘、可编辑，不依赖记忆服务开关）+ 长期记忆
  （对标 ReMe），含召回测试与 wikilink 图谱。
- **Agent 化**：对话主流程换成工具循环，知识库从「唯一通道」降级为**其中一个工具**；
  子 Agent 派生；工具准入策略（allow / deny / ask / sandbox 四档）；
  外部 MCP 服务接进工具表；会话级内核沙箱。
- **工作区**：账号 → Agent → 工作区（项目）→ 会话的归属链；沙箱与工作区是两件事。
- **能力层**：技能（`SKILL.md` 渐进式展开）+ 预装技能包与门控。
- **联网**：`web_search` / `web_fetch` 作为工具进对话。
- **Office 产出**：docx / xlsx / pptx / pdf 作为交付物导出。
- **检索改进**：多轮检索的收益判断、并行、库概况、可重试；分段问题生成提升召回；
  文档列表真分页（下推到 SQL）；**同一片段不再占两个引用号**（一轮里多查几次时按
  `chunk_id` 去重，且已给出的编号不重排）。
- **对话过程如实呈现**：工具步数用尽不再假装跑完——如实标成降级并给重试入口；
  检索那一步带回几段**新**资料（含 0）也如实报出来，界面据此显示"本轮没找到新资料"。
- **可观测性**：增量迁移机制、文档阶段事件时间线、负载面板、列表分段进度条。

### 界面与文案

- 全站照 Kimi Work 重排（侧栏两节清单、能力页 / 插件页、输入框与工具栏），
  引入可调字号系统与 Kimi 实测值的设计 token 体系。
- 全站文案治理：删掉「这一页是什么」的解释性小字、去掉同屏重复标题、统一术语；
  该规则已挂进 `scripts/check_layering.py`（规则 U1）由 CI 强制。
- **逐处对照 kimi.com 对话页**（在浏览器里量渲染结果，不是解析它的样式表变量）：
  左下侧边栏去掉两道通栏分割线与那个外框、账号行与上方导航对齐到同一条 8px 起始线、
  头像换成 28px 圆；导航项 14px / 图标 18px；「新建会话」改回侧栏里唯一有底有框的一颗；
  下拉菜单圆角 16、项高 36、主行恢复主文字色；弹窗去掉描边与投影、头部带 72px；
  输入框底色由"比画布暗一档"改为"比画布亮一档"（层级方向原来反了）。
  取值依据见 [docs/调研/Kimi-界面逐处对照-v0.1.md](docs/调研/Kimi-界面逐处对照-v0.1.md)，
  规范相应升到 [v0.14](docs/规范/前端设计规范-v0.14.md)。
- **新建工作区改成弹窗**（原先是把整页切成"新建态"，要离开你在的地方，
  且表头那颗「保存」同时管着新建与编辑两件事）。工作区的根目录是 Agent 的
  文件操作边界、砍不掉，所以"单字段"照搬不了 kimi.com 的做法；形态取弹窗：
  同样专注，但不占一次导航。

### 工程

- 前端测试由 138 增至 **526**（59 个文件，本地实测通过）；后端用例增至 **2176 条**
  （这个数是 `pytest --collect-only` 口径，0.1.0 那条记的是 passed 口径，两者不可直接相减）。
  本机实测 **2169 通过 / 7 跳过**（跳过全是环境项：5 条要真实对象存储、1 条要 Linux 隔离后端、
  1 条要符号链接权限）。后端真跑需要一个独立的 PostgreSQL 测试库 `KYLAB_TEST_DATABASE_URL`，
  未配置时依赖仓储的用例会**整体跳过**而不是静默变绿。
- 删掉旧 Agent 链路（约 600 行不可达代码 + 一个改了不生效的设置项 `chat.agent_max_rounds`），
  连同 26 条只覆盖它的用例；净减约 790 行。
- REST 端点增至 **148** 条（`docs/规范/API-接口规范-v0.1.md` 由脚本从真实 OpenAPI 生成并有测试核对）；
  MCP 工具增至 **18** 个。
- 分层检查扩到 `L1`–`L6`（新增：业务层依赖协议层、`app/` 根下的游离模块）/
  `A1`（异步端点假异步）/ `T1` / `S1` / `U1` / `V1`（手写版本号一致）与语法错误。
- **接口类型改为从 OpenAPI 派生**（对话/文档/知识库/目录/数据源/存储概览等最常读的
  响应类型 + `DocumentStage` / `DataSourceKind` 两个枚举），后端的字段说明也随契约
  一起进类型（`use_attribute_docstrings`）。
- **前端类型与后端 schema 的契约进了门禁**：`frontend/src/api/schema.d.ts` 由
  `scripts/gen_api_types.py` 从 OpenAPI 生成，`lint` / `check-backend` 用 `--check`
  核对——后端改了 schema 而生成物没跟上时门禁直接红（新增 devDependency
  `openapi-typescript`）。

---

## [0.1.0] — 2026-09-11

首个可用版本。定位：**局域网内的知识库问答**——把散落的文档收进来，
用自己的模型提问并拿到带原文引用的回答。

### 能力

- **摄入流水线**：上传 → 逐文件探测（文本层 / 扫描件 / 版式）→ 解析 → 切块 →
  向量化 → 索引。云端解析（MinerU / PaddleOCR）与本地解析按文件动态路由。
- **检索**：向量 + BM25 双通道，RRF 融合，可选 rerank；检索调试台可复核效果。
- **对话**：流式回答，**强制引用原文**（`[n]` 对应下方出处）。
  提示词注入缓解：资料块定界 + 声明"非指令" + 打散文档内的同形标记。
- **对话留存**：会话与消息落库、标题取首轮提问、**引用存快照**
  （回看时依据的仍是当时那几段）。
- **文档阅读**：双视角——渲染件（Markdown / PDF / 图片）与切块预览。
  行内 LaTeX 清理（真实医学语料里 26% 的切块含 LaTeX 源码）。
- **切块人工干预**：编辑（重新向量化）/ 禁用（不参与检索但保留）/
  删除（连同索引与向量）。解析器一定会出错，这是质量的最后兜底。
- **数据源**：RSS / Atom 订阅与单页 HTML，条件 GET 增量拉取，正文提取剥离导航与广告。
- **生命周期**：删除前给出影响清单；文档删除进回收站保留 7 天，可恢复。
- **模型注册器**：供应商 → 模型目录 → 在各用途面板（对话 / 向量化 / 重排）登记并选定默认。
  **嵌入模型是知识库属性**：建库时按库选，不同库可用不同模型，各自的向量空间互不污染。
- **用量统计**：token 与调用量，按本地日历日 / 用途 / 模型聚合。
  **刻意不算钱**（价目表必然过期）；数字来源分**实测 / 估算 / 未上报**三态。
- **鉴权**：两种凭据——登录会话（`kylab_st_`，控制台唯一入口，管理员 / 成员两档角色）
  与 API Key（给外部程序，碰不到服务端凭据）。**强制先建管理员**，
  没有"未启用鉴权"这种形态；会话与密钥都只存哈希，签名下载 URL 带过期时间。
- **多人共用**：账号体系 + 知识库分享。私有是默认，owner 按登录名授出读 / 写两档，
  被分享者不能再外授；越权按 `403` / 越主按 `404` 收口。
  首个账号由首次设置向导创建，**不开放注册**。
- **可观测性**：任务健康判据（`stalled` 用租约过期判，而非耗时阈值）+ 运行态总览。
- **大文件强制切分**：超过渠道页数上限的 PDF 按页范围切开、流式顺序调用、
  合并时插页标记，页码一路带到检索结果与引用行。
- **Webhook 事件推送**：`document.indexed` / `.failed` / `.deleted`，
  至少一次投递、HMAC-SHA256 验签（时间戳参与签名防重放）、
  失败也推、4xx 不重试。失败不阻断摄入。

### 工程

- 后端 **1120 个测试**、前端 **138 个测试**，全绿。
- 交接文档：[`docs/计划与记录/交接说明-2026-09-11.md`](docs/计划与记录/交接说明-2026-09-11.md)
  （接手必读：现状、三条命令、诚实欠账清单、纪律与踩过的坑）。
- 门禁：`ruff`、`vue-tsc`、`eslint`、`prettier`、零 emoji 扫描、分层纪律检查。
- 界面审计：四页 × 双主题，文字对比度不达标 0。
- 迁移框架只增不改，版本号连续（当前 v11），启动自动补齐。

### 已知限制

- **容器路线未验证**：开发机无 docker，`deploy/docker-compose.yml` 尚未在
  干净机器上跑通。本地 venv 路线已实测。
- **容量推荐表未填**：计划 T7.3 要求至少两档内存的实测数据，尚未做。
  不编造猜测的并发数——那比不给更糟。
- **WebDAV 数据源未实现**（架构 §14 缓做），接口处明确报"未实现"而非空实现。
- **不支持 JS 渲染的页面**：HTML 数据源抓的是服务端返回的 HTML。
- **配置层没有直接单测**：`runtime_config` + `core/config` + `settings` 端点
  共 721 行，只靠真实链路实测覆盖——**当前最大的质量缺口**。
- **图片未落对象存储**：云端解析器已解析出图片字节，但没写进去，
  `ParseResult.image_ids` 因此为空。
- 评测闭环、嵌入发布、Notion/Confluence 等连接器未做。
- **忘记管理员密码没有 HTTP 应急入口**：正式发布前应补本机 CLI
  （如 `kylab admin reset-password`），它不经过 HTTP，因此不算后门。

---

## 附：版本号约定

- `backend/pyproject.toml`、`frontend/package.json`、`Settings.app_version`（`core/config.py`）
  **三处必须一致**——`/api/v1/health` 与 OpenAPI 文档报的版本要能与镜像标签对上。
- MCP Server 的 `version` **不再手写**：`mcp_server/server.py` 取自 `get_settings().app_version`。
  在这之前它是第四处手写副本（0.1.0 → 0.2.0 这一版才发现），而手写副本只会在发版时漏改。
- 镜像标签取 `KYLAB_VERSION`，默认 `0.2.0`（`deploy/docker-compose.yml` 里 backend / frontend 两处）。
- **发版动作**：改上面三处 + compose 的默认标签；把当时的 `[未发布]` 一节挪成
  `## [x.y.z] — <日期>`；`backend/uv.lock` 里的项目版本由 `uv lock` 自动跟上。

# 界面评审与改进计划 · v0.2

> 日期：2026-09-11
> 状态：**已执行**（六项全部落地，逐条截图核对）
> 范围：第二轮，全部来自用户在真实界面上的批注（截图逐条）。
> 上一轮：`界面评审与改进计划-v0.1.md`（下拉/标签/小字/图标基线，已完成）。
> 依据：《前端设计规范 v0.5》（冻结）。

---

## 0. 逐条对照用户批注

| # | 用户批注（原话） | 位置 | 判定 |
|---|------------------|------|------|
| 1 | 「退出登录应该放到二级菜单」 | 侧栏页脚账号区 | 成立：一个低频且不可逆的操作直接摊在页脚，误点代价高 |
| 2 | 「主题应该放到设置里面去去掉」 | 侧栏页脚 | 成立：主题是"这台机器怎么显示"，属于设置；占一个常驻入口不划算 |
| 3 | 「后端在线 · v0.1.0」应去掉 | 侧栏页脚 | 成立：开发期探针，用户不需要常驻看它；真出问题会有请求报错 |
| 4 | 「测试按钮位置不合理，应该注册的时候设置」 | 设置 → 模型 → 用途分配 | 成立：按钮挤在行尾被裁掉；且"这个模型能不能用"属于注册环节，不属于每次绑定用途 |
| 5 | 「所有的下拉框样式都没改」 | 全局 | 成立：换成了 AppSelect，但仍是**原生外观**（`appearance: auto` + 系统箭头），看起来像没动 |
| 6 | 「大小不一」 | 设置 → 供应商登记表单 | 成立且是**根因级**问题：下拉 32px，输入框实测 **43px**（padding + 行高撑出来），两者不同高 |
| 7 | 「滚动条样式没改」 | 设置弹窗内容区 | 成立：默认 OS 滚动条，宽且带按钮，与整体克制风格不符 |
| 8 | 「没有引用我的 svg 图标库」 | 设置 → 左侧分组菜单 | 部分成立：图标基线已换成 Remix（同一集合），但设置分组等大量位置**是纯文字、没有图标**，看不出用了图标库 |

---

## 1. 根因（比逐条修更重要）

三条问题其实是**同一个**：控件的高度与外观没有一处定义。

- `AppInput` 写的是 `min-height: 32px` + `padding: 8px 12px`，字体 15px、行高 1.65
  → 实高 `8+8+2+24.75 ≈ 43px`。规范 §7.4 写的是「输入框 `min-height: 32px`，与按钮同高」，
  **交付值 43px 与规范承诺的 32px 不一致**——和上一轮"规范写 4.5:1、交付 2.83:1"是同一类失效。
- `AppSelect` 只有 `min-height: 32px; padding: 0` → 实高 32px。
  两者放一行，差 11px，肉眼一眼看出"大小不一"。
- 外观上，原生 `<select>` 只加边框仍是系统渲染（系统箭头、系统内边距），
  所以"换了组件"但"样式没变"。

**结论**：需要一个**控件尺寸 token**，让输入框/下拉/按钮共用同一个高度定义，
而不是各自用 padding 凑。

---

## 2. 改进项

### P1 控件统一（用户点名的"大小不一""下拉没改"）

1. `base.css` 增加 `--control-height: 32px`，并写清它是**控件唯一高度口径**。
2. `AppInput` 单行改为 `height: var(--control-height)` + `padding: 0 var(--space-3)`；
   文本域保持多行（`min-height` + `padding`），不受此约束。
3. `AppSelect` 改为**定制外观**：
   - `appearance: none` 去掉系统箭头；
   - 右侧自绘 Remix `arrow-down-s-line` 雪佛龙（绝对定位、`pointer-events: none`）；
   - 补 hover / focus 态；依然是**原生 `<select>`**（键盘、触屏、无障碍不重造）。
4. 三个控件（输入/下拉/按钮）同高同圆角，排成一行时基线一致。

### P1 侧栏页脚收敛（批注 1/2/3）

- 删掉「后端在线」状态行与其探活代码（不再有消费者）。
- 删掉主题切换按钮（移入设置）。
- 「退出登录」收进账号的**二级菜单**：点账号行展开，菜单里只有「退出登录」。
  仍用原生 `<details>`（键盘可展开、Esc 收起），菜单向上弹出。
- 「设置」保留为页脚常驻项——它是入口，不是动作。

### P1 主题移入设置（批注 2）

- `useTheme` 增加 `setTheme('light' | 'dark' | 'system')`：`system` 清掉本地选择、
  回到跟随系统（与 `index.html` 首屏脚本共用同一键，不闪烁）。
- 设置 → 外观改为「主题」+「正文字号」两块，主题三档用与字号相同的
  `scale-picker` 卡片式选择器。

### P2 滚动条（批注 7）

- 全局细滚动条：Firefox `scrollbar-width: thin` + `scrollbar-color`；
  WebKit `::-webkit-scrollbar` 8px、滑块 `--border-strong`、轨道透明、圆角。
  颜色只引主题变量，深色下自动跟随。

### P2 模型配置：测试移到注册环节（批注 4）

- 后端新增 **`POST /model-registry/providers/{id}/test`**：用**该供应商已存的凭据**
  真发一次最小请求（llm 走一次最短对话、embedding 走一次 embed、rerank 走一次 rerank），
  返回可用/失败原因。取该供应商下**第一个已登记模型**作为模型 ID；没有模型则明确提示
  "先登记一个模型再测试"。
- 前端：**去掉用途分配行里的「测试」按钮**（布局挤压缩窄的直接原因），
  改到供应商卡片上（与「编辑 / 删除」同级）；供应商登记表单里也放一个
  「测试连接」——若还没保存，先保存再测（避免"草稿探测"这种额外凭据面）。
- 用途分配行只剩「标签 + 状态 + 下拉」，不再被按钮挤。

### P2 图标落到该落的地方（批注 8）

- 设置左侧八个分组各配一枚 Remix 图标（模型/向量化/对话模型/服务配置/存储配置/外观/用户/系统与安全）。
- 常见动作补齐图标：添加供应商/添加模型 `add-line`、编辑 `edit-line`、
  删除 `delete-bin-line`、测试 `shield-check-line`（"验活"语义）、
  下载 `download-line`、外部链接 `external-link-line`。
- 原则不变：**图标是辨认加速，不是装饰**——纯文字已经够清楚的地方不加图标。

---

## 3. 执行顺序与状态

| 序 | 项 | 级别 | 状态 |
|----|----|------|------|
| 1 | 控件高度 token + AppInput/AppSelect 统一 + 自绘雪佛龙 | P1 | **完成** |
| 2 | 侧栏页脚收敛（二级菜单 / 去主题 / 去状态） | P1 | **完成** |
| 3 | 主题移入设置（三档，含跟随系统） | P1 | **完成** |
| 4 | 全局细滚动条 | P2 | **完成** |
| 5 | 供应商探活端点 + 测试按钮迁移 | P2 | **完成** |
| 6 | 设置分组与动作图标 | P2 | **完成** |

完成后：跑门禁 + 截图逐条核对批注位置。

---

*v0.2，随本轮执行更新；v0.1 保留存档。*


---

## 4. 执行结果（实测）

| 批注 | 改前（实测） | 改后（实测） |
|------|--------------|--------------|
| 大小不一 | 输入框 43px、下拉 32px | 两者均 **32px**（`--control-height`） |
| 下拉没改 | `appearance: auto`（系统箭头） | `appearance: none` + 自绘 Remix 雪佛龙 |
| 测试按钮 | 每个用途行尾一颗，窄窗口被裁 | 用途行 **0 颗按钮**；移到供应商卡片，后端口 `GET {base_url}/models`（不计费） |
| 退出登录 | 页脚常驻按钮 | 账号二级菜单（修改密码 / 退出登录） |
| 主题 | 页脚切换按钮 | 设置 → 外观，三档（跟随系统 / 浅色 / 深色） |
| 后端状态 | 页脚常驻一行 | 已删 |
| 滚动条 | 系统默认 | 全局细滚动条（8px，主题色） |
| 图标 | 设置分组纯文字 | 八个分组各一枚 Remix 图标 |

**顺带修掉一个真 bug**：`initTheme()` 从未被调用——系统深色偏好下，首屏脚本把页面
渲染成深色，而 composable 的状态停在浅色，两者不一致（主题选择器也会高亮错档）。
现在 App 启动时对齐（与 `--font-scale` 同一处理）。


---

## 5. 第三轮（用户续批）

| 批注 | 做法 |
|------|------|
| 「滚动条要圆角样式」（给了 Kimi / wekora 截图） | 滑块改**悬浮胶囊**：`background-clip: content-box` + 4px 透明边框把滑块收窄，`border-radius: 999px`。只写 `border-radius` 不够——滑块会撑满轨道宽，看起来还是一根方条 |
| 「菜单小字解释太多了，几乎每个菜单都有」 | 设置左栏改为 **分组标题 + 图标 + 单行标签**（对齐 Kimi 桌面端 / wekora 设置）：模型 / 服务 / 账户 / 偏好四组，**每项下面不再挂小字**；分组标题是菜单里唯一的小字 |
| 同上（内容区也被圈了两处长说明） | 精简设置内容区的小字：只留"不写会做错"的那一句，删掉复述与设计理由。例：对话模型段的整段说明 → 一句"没配好时「对话」直接报错，不编造答案"；字号段的三行解释整段删除 |

**顺带**：门禁与 review 的范围纪律写进《开发计划》§12.24——**按改动范围执行**，
只改前端就跑 `scripts/check-frontend.{sh,ps1}`（新增），不再顺带重跑后端 1100+ 用例。


---

## 6. 第四轮：下拉改成自绘（用户：「依旧不好看，我更喜欢 Kimi 的」）

**为什么前两版都没解决**：第一版各处原生 `<select>` 各写一套样式；第二版统一到
`AppSelect` 但**内核仍是原生 `<select>`**——系统渲染的弹出列表改不了选项行高、
圆角、勾选与悬停，所以看着"就是没换过"。这次把触发器与列表都自己画。

**Kimi 观感的三个要点（逐一落地）**

| Kimi 的元素 | 落地 |
|-------------|------|
| 触发器是**灰底圆角块**，不是"白底 + 1px 边框" | `--bg-subtle` 填充、透明边、`--radius-row` 圆角；hover 转 `--bg-hover` |
| 聚焦/展开时有**细边 + 光晕** | `--accent` 1px 边 + `--accent-soft` 3px 光晕；箭头翻转 180° |
| 弹出列表是**白卡 + 阴影**，当前值右侧**打勾** | `--bg-surface` + `--radius-overlay` + `--shadow-popover`；选项行高 36，`IconCheck` 标当前值 |

**工程上的两个坑（都已修）**

1. **浮层不能 Teleport 到 body**：设置弹窗是原生 `<dialog>`，处于 top layer，
   挂在 body 上的 `z-index: 60` 浮层会被整个盖住——实测选项完全看不见。
   改回原地渲染 + `position: fixed`（既不被 `overflow: auto` 裁剪，又与弹窗同层）。
2. **`scrollIntoView` 在 jsdom 是未实现桩**：调用会往虚拟控制台抛错、弄脏测试输出。
   改成按 `offsetTop/scrollTop` 手算滚动位置；顺带免掉 `CSS.escape`（jsdom 无 `CSS` 全局）。

**无障碍**：`role=combobox/listbox/option` + `aria-activedescendant`（焦点始终留在
触发器，键盘与服务阅读器共用同一份高亮状态），方向键/Home/End/Enter/Esc/Tab 全覆盖，
点外部关闭（捕获阶段监听 `pointerdown`）。新增 8 项单测钉住这些行为。

**规范影响**：触发器圆角与填充式外观属规范级变更 → 新增《前端设计规范 **v0.6**》
（只改 §7.1 一行与 §7.4 一条），v0.5 标注为已被取代保留。


---

## 7. 第五轮：小字说明改为「圆圈问号」

用户：「像这些小字都可以不要。实在有必要阐述的，可以在旁边配圆圈问号，移上去查看。」

**新增 `InfoTip`**（`components/ui/InfoTip.vue`）：`question-line` 图标 + 悬停/键盘聚焦
展开的说明浮层。三条实现取舍：
- 用 `button` + `role="tooltip"` + `aria-describedby`，**不用原生 `title`**（延迟长、
  样式不可控、触屏看不到）；
- `focus`/`blur` 同样展开收起——不能只有 hover；
- 浮层 `position: fixed`（与 AppSelect 同一手法，避免被设置弹窗的 `overflow` 裁掉）。

**处理范围**：向量化兜底、重排跳过、对话模型、服务节点、存储、外观、用户、修改密码
共 8 处说明改为标题/标签旁的问号；「初始密码需转告对方」「未配置时的兜底段」
「令牌只存本机」等 5 处直接删除（不写也不会做错）。
**保留常驻可见**的只有破坏性确认（删除账号会置空归属）与条件性警告（开了深度思考）。

踩到的坑：`InfoTip` 在 `SettingsModal` 里**忘了 import**，Vue 静默跳过该组件、
页面照常渲染只是没有问号——正是交接文档 §6「前端坑」里记的那一条。已补。

**规范影响**：新增《前端设计规范 **v0.7**》（只加 §7.4 一条文字纪律），v0.6 标注为已被取代。

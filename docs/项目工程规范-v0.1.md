# 项目工程规范 · v0.1

> 适用：知识库产品全仓库（Python FastAPI 后端 + Vue 3 前端 + 设计文档 + 测试）
> 日期：2026-09-10
> 状态：已被 v0.2 取代（保留存档，勿再作为执行依据）
> 变更摘要：初版。定下 Monorepo 目录结构、后端分层纪律与命名、前端目录与风格约束、测试存放与执行规范、密钥与提交规范、新增文件决策树。

---

## 1. 仓库总体结构（Monorepo）

```
<repo-root>/
├── docs/                  # 设计与规范文档（本文件及架构文档的家）
├── backend/               # Python 后端（FastAPI + MCP Server）
├── frontend/              # Vue 3 + Vite Web 控制台
├── tests/                 # 跨端测试（E2E、集成测试）
├── scripts/               # 开发/部署脚本
├── deploy/                # Docker Compose 等部署产物
├── .github/workflows/     # CI（如用 GitHub）
├── README.md
└── LICENSE
```

**铁律：任何文件都有唯一归属目录，不允许在仓库根目录散落临时文件。**

---

## 2. 设计文档规范（docs/）

| 项 | 规范 |
|----|------|
| 存放 | 一律放 `docs/`，按主题单文件 |
| 命名 | `<主题>-<类型>-v<主.次>.md`，如 `架构设计-v0.2.md`、`前端设计规范-v0.1.md`、`项目工程规范-v0.1.md` |
| 版本 | 讨论中改动升次版本（v0.1→v0.2）；定稿冻结为 v1.0；旧版本**保留不删**，新文件替代 |
| 头部 | 必须含：版本号、日期、状态（讨论稿/基线/冻结）、变更摘要 |
| 语言 | 中文为主；代码标识符、接口名、路径保持英文原样 |

当前文档清单：`架构设计`（产品与技术架构）、`前端设计规范`（视觉与组件）、`项目工程规范`（本文件）。后续新增：`API 接口规范`、`解析插件开发指南` 等。

---

## 3. 后端代码规范（backend/）

### 3.1 目录结构

```
backend/
├── app/
│   ├── main.py                # FastAPI 入口
│   ├── api/                   # REST 路由层（协议适配，无业务逻辑）
│   │   └── v1/                # API 版本化，路径即版本
│   ├── mcp_server/            # MCP Server（stdio / streamable HTTP）
│   ├── core/                  # 配置、日志、鉴权、异常
│   ├── services/              # 业务服务：kb / ingest / retrieval / connector
│   ├── pipeline/              # 摄入流水线状态机
│   ├── parsers/               # ParserProvider 接口与各实现
│   │   ├── base.py            # ParserProvider 协议
│   │   ├── probe.py           # 文件探测（文本层覆盖率判定）
│   │   ├── mineru_cloud.py
│   │   ├── mineru_local.py
│   │   ├── paddleocr_api.py
│   │   └── office.py          # pandas/openpyxl/python-docx 系
│   ├── storage/               # Repository 抽象 + SQLite 实现
│   │   ├── base.py            # VectorStore / MetaStore 接口
│   │   └── sqlite_impl/       # 唯一实现；PG 实现未来平级新增
│   ├── models/                # Pydantic / ORM 模型
│   └── workers/               # asyncio 任务队列与消费者
├── tests/                     # 后端测试（见第 5 节）
├── pyproject.toml
└── README.md
```

### 3.2 命名

| 对象 | 规则 | 示例 |
|------|------|------|
| 文件名 | snake_case | `mineru_cloud.py` |
| 类 | PascalCase | `MinerUCloudParser` |
| 函数/变量 | snake_case | `probe_text_coverage()` |
| 常量 | UPPER_SNAKE | `MAX_SPLIT_PAGES = 150` |
| API 路径 | kebab-case，带版本 | `/api/v1/knowledge-bases/{id}/documents` |
| MCP 工具 | snake_case 动词开头 | `upload_document`、`get_document_status` |
| 解析器实现类 | `<引擎><节点>Parser` | `PaddleOCRApiParser` |

### 3.3 分层纪律

- `api/` 和 `mcp_server/` 只做协议适配，**禁止写业务逻辑**——两种接入共用 `services/`；
- `services/` **禁止直接写 SQL**，必须经 `storage/` 的 Repository 接口（PG 迁移后门的保险费）；
- `parsers/` 各实现只依赖 `base.py` 的 `ParseResult` 中间产物，互不引用。

---

## 4. 前端代码规范（frontend/）

### 4.1 目录结构

```
frontend/
├── src/
│   ├── assets/              # 全局样式、主题 CSS 变量
│   │   └── themes/          # light.css / dark.css
│   ├── components/          # 通用组件（PascalCase.vue）
│   │   └── icons/           # 内联 SVG 图标组件（Lucide 基准）
│   ├── views/               # 页面：知识库树 / 文件列表 / 任务中心 / 调试台 / 设置
│   ├── composables/         # use 前缀，camelCase
│   ├── api/                 # 后端接口封装（与 /api/v1 对齐）
│   ├── stores/              # Pinia
│   ├── router/
│   └── main.ts
├── tests/                   # 前端测试（见第 5 节）
├── index.html
├── vite.config.ts
└── package.json
```

### 4.2 命名

| 对象 | 规则 | 示例 |
|------|------|------|
| 组件文件 | PascalCase.vue | `FileTreeItem.vue`、`ChunkHitCard.vue` |
| 组合式函数 | camelCase，`use` 前缀 | `useTaskPolling.ts` |
| 工具/普通 TS | camelCase.ts | `formatBytes.ts` |
| CSS 变量 | kebab-case，带语义前缀 | `--bg-canvas`、`--text-secondary` |
| SVG 图标组件 | `Icon<名称>.vue` | `IconFilePdf.vue`、`IconRss.vue` |
| 路由路径 | kebab-case | `/knowledge-bases/:id/documents` |

### 4.3 风格纪律（引自《前端设计规范》，此处为工程约束）

- **禁止 emoji 出现在任何源码字符串中**（lint 规则拦截，见第 6 节）；
- 图标只能引 `components/icons/`，禁止外链图标库 / iconfont / 图片图标；
- 颜色只能引用主题 CSS 变量，禁止硬编码色值（语义色除外且仅用于状态）。

---

## 5. 测试规范（重点）

### 5.1 测试文件存放位置

| 测试类型 | 位置 | 说明 |
|----------|------|------|
| 后端单元测试 | `backend/tests/unit/` | 与被测模块**镜像同构**：`app/parsers/probe.py` → `tests/unit/parsers/test_probe.py` |
| 后端集成测试 | `backend/tests/integration/` | 跨模块：流水线状态机、存储层、API 端到端 |
| 后端测试夹具 | `backend/tests/fixtures/` | 样例 PDF/DOC/MD/CSV、mock 的 MinerU 响应等 |
| 前端组件测试 | `frontend/tests/unit/` | 与 `src/components/` 镜像同构 |
| E2E 测试 | `<repo-root>/tests/e2e/` | 跨前后端全链路（Playwright），放仓库根级 `tests/` |
| 性能/容量基准 | `backend/tests/bench/` | 向量检索延迟、批量摄入吞吐（如 45 万 chunk 刻度验证） |

**铁律**：
1. 测试代码**绝不进入** `app/` 或 `src/` 源码目录（不散落 `__tests__`、不混放 `test_*.py`）；
2. 测试文件命名 = `test_` + 被测文件名（后端）/ `<组件名>.test.ts`（前端）；
3. 夹具文件用真实小样本，禁止把大文件（>5MB）提交进仓库，大样本放 `fixtures/README.md` 指明的下载方式。

### 5.2 测试执行位置

| 场景 | 命令 | 执行位置 |
|------|------|----------|
| 后端单元+集成 | `cd backend && pytest tests/ -m "not bench"` | 本地开发 & CI 每次提交 |
| 后端基准 | `cd backend && pytest tests/bench/` | 手动触发 & 发布前 |
| 前端组件 | `cd frontend && pnpm test` | 本地开发 & CI 每次提交 |
| E2E | `pnpm --dir tests/e2e test`（先起本地服务） | CI 主分支 & 发布前 |
| 覆盖率 | `pytest --cov=app` / `vitest --coverage` | CI 产出报告 |

- **CI 是最终裁判**：本地通过不算数，PR 合并前 CI 全绿；
- 云端解析类测试（MinerU/PaddleOCR 真实 API）一律打 `cloud` 标记，CI 默认跳过（用 fixtures 里的 mock 响应），避免消耗免费额度与网络抖动导致红 CI；
- 集成测试使用**独立临时 SQLite 文件**（tmp 目录），禁止触碰开发库。

### 5.3 验收关联

架构文档中的关键承诺应有对应测试：`>1000 页强制切分`、`embedding 模型 ID 不一致禁止切换`、`hash 去重提醒`、`级联删除影响清单`——每条对应至少一个集成测试用例。

---

## 6. 其他工程规范

| 项 | 规范 |
|----|------|
| 配置文件 | 后端 `backend/.env.example`（提交模板，密钥不入库）；用户配置运行期落 SQLite |
| 密钥 | 任何 token/API key 禁止提交；`.gitignore` 必须含 `.env`、`*.db`、`data/` |
| 脚本 | 开发/部署脚本统一放 `scripts/`，命名 `<动词>-<对象>.sh/.ps1`（双平台各一份） |
| 部署产物 | `deploy/docker-compose.yml`，镜像标签与版本号一致 |
| 提交信息 | `<类型>: <简述>`，类型限 `feat/fix/docs/test/refactor/chore` |
| 分支 | `main` 保护；功能分支 `feat/<简述>`，修复 `fix/<简述>` |
| Lint | 后端 ruff；前端 eslint + prettier + **自定义 no-emoji 规则** |
| 依赖 | 后端 `pyproject.toml`（uv 或 pdm 锁定）；前端 `pnpm` + lockfile 必须提交 |
| 运行时数据 | 开发期数据目录 `data/`（SQLite、上传文件、Markdown 产物），已在 `.gitignore` |

---

## 7. 新增文件的决策树

```
要写一个新文件？
├─ 是设计/规范文档？ → docs/，按 §2 命名
├─ 是 Python 源码？   → backend/app/ 下按分层归属（§3.1），禁止放别处
├─ 是前端源码？       → frontend/src/ 下按 §4.1 归属
├─ 是测试？           → 按 §5.1 表格对号入座，镜像同构
├─ 是脚本？           → scripts/
├─ 是部署配置？       → deploy/
└─ 以上都不是？       → 先想清楚该不该存在，再在工程规范里补一类
```

---

*v0.1，仓库初始化时即生效。*
